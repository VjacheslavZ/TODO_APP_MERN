extends ../base

block title
    | Index page 2

block main
    .auth
        form.form__auth
            .input__wrap
                i.fas.fa-user
                input.input__default(placeholder='Name' name='name' required='required')
                .invalid-feedback Reason description
            .input__wrap
                i.fas.fa-user
                input.input__default(placeholder='Email' name='email' required='required')
                .invalid-feedback Reason description
            .input__wrap
                i.fas.fa-lock
                input.input__default(placeholder='Password' name='password' required='required')
                .invalid-feedback Reason description
            .input__wrap
                i.fas.fa-lock
                input.input__default(placeholder='Password confirm' name='password_confirm' required='required')
                .invalid-feedback Reason description

            button.button__form-action(type='submit') Join
